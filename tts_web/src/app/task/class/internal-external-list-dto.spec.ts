import { InternalExternalListDTO } from './internal-external-list-dto';

describe('InternalExternalListDTO', () => {
  it('should create an instance', () => {
    expect(new InternalExternalListDTO()).toBeTruthy();
  });
});
